
.page-header
  %h1 Edit Profile

= form_for @user do |f|
  .form-group
    = f.label :full_name 
    = f.text_field :full_name, class: "form-control"
  .form-group
    = f.label :avatar 
    = image_tag(@user.avatar_url, class: "thumbnail") if @user.avatar?
    = f.file_field :avatar
    = f.hidden_field :avatar_cache

  = f.submit "Update Profile", class: "btn btn-primary"